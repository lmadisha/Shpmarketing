# Operations API Environment Configuration (Development)
# Copy this to operations-api/.env for local development

PORT=5001

# Database Configuration (for development)
OPS_DB_USER=frostlink
OPS_DB_PASSWORD=frostlink-dev-password
OPS_DB_HOST=localhost
OPS_DB_PORT=5432
OPS_DB_NAME=frostlink
DATABASE_URL=postgresql://frostlink:frostlink-dev-password@localhost:5432/frostlink

# Authentication & Security (REQUIRED - change in any deployed environment)
JWT_SECRET=your-secure-jwt-secret-dev-only-change-this
MOBILE_API_KEY=your-mobile-api-key-dev-only-change-this

# CORS Configuration
CORS_ORIGIN=http://localhost:5173,http://127.0.0.1:5173

# Email provider: "smtp" (default) or "ses"
EMAIL_PROVIDER=ses

# SMTP (used when EMAIL_PROVIDER=smtp or unset) — leave SMTP_HOST blank to disable
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
APP_URL=http://localhost:5173

# AWS SES (used when EMAIL_PROVIDER=ses)
AWS_REGION=us-east-1
SES_FROM=
# Access key auth — leave both blank to use IAM role (EC2/ECS instance profile)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=

# Reserved for existing modules database (phase 2 backend integration)
ANALYTICS_DB_USER=postgres
ANALYTICS_DB_PASSWORD=postgres
ANALYTICS_DB_HOST=localhost
ANALYTICS_DB_PORT=5432
ANALYTICS_DB_NAME=shpmarketing_analytics